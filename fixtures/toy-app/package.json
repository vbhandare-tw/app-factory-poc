{
  "name": "toy-app",
  "version": "0.1.0",
  "description": "Tiny target repo the factory builds against in tests and the POC run",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "test": "node --disable-warning=ExperimentalWarning --experimental-strip-types --test",
    "lint": "node scripts/lint.mjs",
    "build": "node --disable-warning=ExperimentalWarning --experimental-strip-types scripts/build.mjs"
  },
  "dependencies": {},
  "devDependencies": {}
}
