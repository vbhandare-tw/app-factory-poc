---
description: TEMP probe — reports the active model (verifies the model frontmatter override)
model: opus
---

In one short line, state the exact model ID you are running as right now (from your system
context) — e.g. `claude-opus-4-8` or `claude-sonnet-5`. Do nothing else.
